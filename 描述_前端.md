# 前端材料说明（frontend/）

纯静态网站，竞赛展示用，由 Render Static Site 托管。

## 文件清单

| 文件 | 作用 |
|---|---|
| `index.html` | 落地首页（浅色框架，简介 + 各模块跳转） |
| `kg.html` | 知识图谱引擎页（核心交互页，对话框查知识） |
| `control.html` | 工艺自迭代演示 |
| `vision.html` | 视声融合演示 |
| `roi.html` | 改造收益测算 |
| `compete.html` | 竞品雷达 |
| `dashboard.html` | 实时看板 |
| `style.css` | 全局浅色样式 |
| `app.js` | 多语言切换等前端逻辑 |
| `chart.umd.js` | Chart.js 4.4.7 本地内联（零 CDN 依赖） |
| `config.js` | ★ **部署配置：后端 API 地址，见下** |

## 与后端对接的关键改动（已做）

- `kg.html` 第 3035 行（原 `fetch('/api/kg')`）已改为：
  ```js
  fetch((window.API_BASE || '') + '/api/kg')
  ```
- 并在 `<head>` 引入 `config.js`。

## ★ 部署后必须做的一步

打开 `frontend/config.js`，把 `window.API_BASE` 从空串改成后端真实地址：

```js
// 改成你的后端地址，结尾不要带斜杠
window.API_BASE = 'https://rongzhi-cuixin-backend.onrender.com';
```

改完**重新推一次前端**（触发 Static Site 重新部署），`kg.html` 才能跨域读到知识图谱。
如果留空 `''`，则走相对路径，仅在「前后端同域」时可用（本分离架构下不可用）。

## Render 部署要点

- 服务类型：**Static Site**，Runtime **static**，Plan **Free**
- `staticPublishPath: .`（即 frontend/ 目录根）
- 纯静态，无需 build 命令
- 拿到地址形如 `https://rongzhi-cuixin-frontend.onrender.com`

## 验证

打开 `https://<frontend>.onrender.com/kg.html`，在对话框输入「莫来石」，应匹配出"高纯莫来石定型砖"（说明 `API_BASE` 填对、跨域打通）。
