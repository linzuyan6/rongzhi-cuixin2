# 后端材料说明（backend/）

轻量 Flask + SQLite 知识图谱服务，竞赛演示用。

## 文件清单与作用

| 文件 | 作用 |
|---|---|
| `app.py` | 后端主程序。路由：`GET/POST /api/kg`（知识图谱读写）、`/admin/login`、`/admin`、`/admin/logout`（管理员后台）。首次运行自动建 SQLite 库并从 `kg_seed.json` 导入种子。 |
| `requirements.txt` | Python 依赖：`Flask==3.1.0`、`Flask-Cors==4.0.1` |
| `kg_seed.json` | 知识图谱种子数据（38 实体 + 用户发布的"高纯莫来石定型砖"共 39 实体、51 关系、7 砖型参数、6 工位）。部署时从它重建数据库，**已固化关键节点**。 |
| `templates/admin_login.html` | 浅色管理员登录页 |
| `templates/admin.html` | 浅色后台：发布新节点表单 + 已有实体列表 |
| `.gitignore` | 忽略 `kg.db`（运行时生成，不进仓库） |

## 相对原始代码的改动（已做，Qoder 无需再改）

1. **去掉前端托管**：`Flask(__name__, static_folder=None)` —— 后端不再托管 HTML，只提供 API + 后台。
2. **加 CORS**：`from flask_cors import CORS` + `CORS(app, resources={r"/api/*": {"origins": "*"}})`，允许前端静态站跨域读 `/api/*`。
3. **根路径改健康检查**：`/` 返回 `{"service":"rongzhi-cuixin-kg-api","status":"ok"}`，便于确认服务存活。
4. **端口已适配 Render**：`port = int(os.environ.get('PORT', 5000))`，读 `$PORT` 环境变量，未写死。

## Render 部署要点

- 服务类型：**Web Service**，Runtime **Python 3**，Plan **Free**
- Build：`pip install -r requirements.txt`
- Start：`python app.py`
- 环境变量：`ADMIN_PASSWORD=linzuyan2007`、`SECRET_KEY=<随机串>`（`PORT` 不填，自动注入）
- 注意：免费版 15 分钟无访问会休眠，首次访问冷启动约 30~50 秒，属正常。

## 接口示例

```
GET  https://<backend>.onrender.com/api/kg        # 公开，返回全量知识图谱 JSON
POST https://<backend>.onrender.com/api/kg        # 需先登录后台，body 为 JSON 新节点
GET  https://<backend>.onrender.com/admin/login   # 后台登录（密码 linzuyan2007）
```
