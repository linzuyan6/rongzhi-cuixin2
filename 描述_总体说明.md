# 熔智淬新 · Qoder 部署材料（总体说明）

> 这是交给 **阿里 Qoder（AI 编程 Agent）** 的部署材料包。按「前后端分离」架构整理好，Qoder 拿去推 GitHub + 连 Render 一气呵成。

## 一、重要事实澄清（先读，避免 Qoder 走偏）

你原来给 Qoder 的提示词里有几个前提和咱们的真实代码**对不上**，我已经在这个包里处理好了，Qoder 不用再改：

| 提示词里的假设 | 真实情况 | 本包已如何处理 |
|---|---|---|
| "前后端分离完整代码" | 原始代码是**合一**的（`app.py` 用 `static_folder='.'` 把前端整包托管） | 已拆成 `backend/` + `frontend/` 两份 |
| "任务1：修改端口监听适配 $PORT" | 端口**已经适配**了（`app.py` 第192行 `os.environ.get('PORT', 5000)`） | 无需再改，保留即可 |
| 没提 CORS | 拆分后前端（静态站）和后端（API）是**不同域名**，必须跨域 | 后端已加 `Flask-Cors`，放开 `/api/*` |
| 前端用 `localhost` 接口 | 实际前端 `kg.html` 用的是相对路径 `/api/kg`（合一模式能通） | 已改为 `window.API_BASE + '/api/kg'`，部署后填后端地址即可 |

## 二、目录结构

```
Qoder部署材料/
├── render.yaml              # 根配置：一个仓库定义「后端API」+「前端静态站」两个服务
├── backend/                 # 材料1：后端（Flask + SQLite + 管理员后台）
│   ├── app.py               # 已改为纯 API（去 static 托管、加 CORS、根路径返回健康检查）
│   ├── requirements.txt     # Flask==3.1.0 + Flask-Cors==4.0.1
│   ├── kg_seed.json         # 知识图谱种子数据（已含"高纯莫来石定型砖"节点）
│   ├── templates/           # 管理员后台页面（admin_login.html / admin.html）
│   └── .gitignore           # 忽略 kg.db（部署时从种子重建）
└── frontend/                # 材料2：前端（纯静态站）
    ├── index.html kg.html control.html roi.html compete.html vision.html dashboard.html
    ├── style.css app.js chart.umd.js
    └── config.js            # ★ 部署后填入后端地址（window.API_BASE）
```

## 三、两个服务的角色

- **后端 `rongzhi-cuixin-backend`**（Render Web Service，Python）
  - 提供 `GET/POST /api/kg`、管理员后台 `/admin`
  - 数据库用 SQLite，首次运行从 `kg_seed.json` 自动建库
  - 拿到地址形如 `https://rongzhi-cuixin-backend.onrender.com`
- **前端 `rongzhi-cuixin-frontend`**（Render Static Site）
  - 托管全部 HTML/CSS/JS
  - `kg.html` 通过 `config.js` 里的 `API_BASE` 访问后端
  - 拿到地址形如 `https://rongzhi-cuixin-frontend.onrender.com`

## 四、交给 Qoder 后的正确操作顺序

1. 把这个 `Qoder部署材料/` 整个目录作为**新仓库内容**推到 GitHub（替换掉之前那个合一的 `site/` 仓库，或新建仓库都行）。
2. Render 连接该仓库，选择**「Blueprint / 读取 render.yaml」**，一次性建好两个服务。
3. **先等后端部署完成**，拿到后端 URL。
4. 把后端 URL 写进 `frontend/config.js` 的 `window.API_BASE`（例如 `https://rongzhi-cuixin-backend.onrender.com`），重新推一次前端（触发前端重新部署）。
5. 验证（见下）。

## 五、验证清单

- [ ] 后端根路径 `https://<backend>.onrender.com/` 返回 `{"service":"rongzhi-cuixin-kg-api","status":"ok"}`
- [ ] 后端接口 `https://<backend>.onrender.com/api/kg` 返回 JSON（含 entities/relations/paramDB）
- [ ] 前端首页 `https://<frontend>.onrender.com/` 正常打开
- [ ] 前端 `kg.html` 输入"莫来石"能匹配出"高纯莫来石定型砖"（说明跨域打通）
- [ ] 后台 `https://<backend>.onrender.com/admin/login` 密码 `linzuyan2007` 能登录

## 六、排错要点（给 Qoder 的备忘）

- **构建失败**：看 Build 日志，多半是 `requirements.txt` 装包失败 → 确认 Flask / Flask-Cors 版本。
- **启动失败 / 端口报错**：确认 `startCommand: python app.py` 且 app.py 读 `$PORT`（已写）。
- **接口 404**：确认访问的是后端域名下的 `/api/kg`，不是前端域名。
- **跨域报错 CORS**：确认后端 `CORS(app, resources={r"/api/*": {"origins": "*"}})` 在；前端 `config.js` 的 `API_BASE` 已填后端真实地址、结尾无斜杠。
- **登录失败**：确认 Render 环境变量 `ADMIN_PASSWORD=linzuyan2007` 已设；或后台 session 过期重登。
